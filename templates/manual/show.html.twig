{% extends 'base.html.twig' %}

{% block title %}Manual{% endblock %}

{% block body %}
    <header class="section-header">
        <h1>Manual</h1>
        <div>
            <a href="{{ path('manual_index') }}">back to list</a>
        </div>
    </header>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ manual.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ manual.name }}</td>
            </tr>
        </tbody>
    </table>

    <footer class="footer-actions">
        <button class=" btn-edit btn-round"><a href="{{ path('manual_edit', {'id': manual.id}) }}">edit</a></button>

        {{ include('manual/_delete_form.html.twig') }}
    </footer>
    

{% endblock %}
